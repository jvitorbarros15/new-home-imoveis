# New Home Imóveis

Responsive Portuguese-language real-estate website: public property search, property detail pages, WhatsApp conversion flows, a financing estimator and a Supabase-backed administration area.

[Portfolio deployment](https://new-home-imoveis.vercel.app)

> **Portfolio project.** This is an independent demonstration build. It is not operated by, affiliated with or endorsed by the company whose name and visual identity it takes as a reference. Contact details shown in the interface are illustrative. The deployment is `noindex`/`Disallow` so it never competes with the real company in search results.

## What is included

- Public home, listing search, property detail, company, financing and privacy pages
- Listing search with filters (purpose, type, region, price range, bedrooms) and pagination
- Contact requests persisted to the database before the WhatsApp hand-off, so a lead is never lost
- Favourites saved per browser, with a page that resolves them against live listings
- First-party, anonymous conversion counters — no third-party analytics or tracking cookies
- Supabase authentication, TOTP two-factor, listings CRUD and image storage
- Row Level Security limiting every write to an explicit administrator allowlist
- All JavaScript self-hosted and bundled with esbuild; no CDN script tags

## Run locally

Requirements: Node.js 20+.

```bash
npm install
cp .env.example .env      # fill in the Supabase values
node --env-file=.env scripts/build.mjs
npx serve v1
```

Open the local URL printed by `serve`. Re-run the build after editing a JSX source file.

`v1/config.js` and `v1/imovel.html` are generated by the build and are not tracked in Git.

## Supabase setup

1. Create a Supabase project and run every SQL file in `supabase/migrations/` in numeric order.
2. Create the administrator in Supabase Authentication.
3. Copy that user's UUID and run:

```sql
insert into public.admin_users (user_id)
values ('YOUR-AUTH-USER-UUID');
```

4. Set `SUPABASE_URL` and `SUPABASE_ANON_KEY` in the deployment environment.

The anonymous key is expected in browser code; a service-role key is not. Database and Storage policies are the security boundary. Never put a service-role key in this repository or in a frontend environment variable.

### Required dashboard configuration

Two protections cannot be expressed in code and must be enabled in the Supabase dashboard:

- **Attack protection** — enable Cloudflare Turnstile under *Authentication → Attack Protection*, and set `TURNSTILE_SITE_KEY` to the matching site key. The login form renders the challenge and passes the token to `signInWithPassword`; without this, only Supabase's default auth rate limits apply.
- **Multi-factor authentication** — enable TOTP under *Authentication → Providers → MFA*, then enrol an authenticator from the admin panel's *Segurança* view. Until a factor is enrolled, a single password guards every listing.

## Security posture

- Content Security Policy, HSTS, `frame-ancestors`, `X-Content-Type-Options` and a restrictive `Permissions-Policy` on every route (`vercel.json`); the admin route additionally sets `frame-ancestors 'none'` and `no-store`.
- `script-src 'self'` — React and supabase-js are bundled from npm, so a CDN compromise cannot inject script into the admin session.
- Row Level Security on `properties`, `leads`, `events`, `admin_users` and Storage objects; every write path goes through `public.is_admin()`, a `security definer` function with a pinned `search_path`.
- Anonymous visitors may insert into `leads` and `events` and read nothing back; only administrators can read them.
- Uploads are re-encoded to WebP in the browser, and the bucket enforces its own MIME allowlist and size limit.

## Repository structure

```text
api/imovel.js           Serves /imovel with per-listing Open Graph metadata
templates/imovel.html   Shell used by that function (and copied to v1/ for local dev)
scripts/build.mjs       Production bundle build
v1/                     Static site and JSX source
v1/dist/                Generated bundles (ignored by Git)
supabase/migrations/    Database, Storage and RLS setup
```

## Deployment

`vercel.json` builds the bundles, serves `v1/`, enables clean URLs, rewrites `/imovel` to the metadata function and applies the security headers above.

Social crawlers do not execute JavaScript, so `/imovel?code=...` is served by `api/imovel.js`, which reads the listing and injects the real title, description and cover image before returning the shell. Without it, every shared link would show the same preview.
